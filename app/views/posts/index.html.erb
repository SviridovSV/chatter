<h1>All Posts</h1>

<ul id="posts">
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post) %> Post rate <%= post.total_rate || 'is absent' %> Comments (<%= number_of_comments(post) %>)
    </li>
  <% end %>
</ul>


