<h3>Post a Comment</h3>
<%= form_for [ @post, @comment ] do |f| %>

  <p>
    <%= f.label :body %><br/>
    <%= f.text_area :body %>
  </p>
  <%= f.hidden_field :user_id, value: current_user.id %>

  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>
